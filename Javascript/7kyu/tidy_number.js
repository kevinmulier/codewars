const tidyNumber = (n) => Number(n.toString().split("").sort().join("")) == n;

//   Definition
// A Tidy number is a number whose digits are in non-decreasing order.

// Task
// Given a number, Find if it is Tidy or not .
